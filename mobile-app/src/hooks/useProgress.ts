/**
 * Re-export useProgress hook for convenient imports
 * This allows importing from hooks folder: import { useProgress } from '../hooks'
 */

export { useProgress } from '../context/ProgressContext';
